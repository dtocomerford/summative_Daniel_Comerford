<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dbinteractionpractice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DbInteractionPractice">
        <activity
            android:name=".museumCafe"
            android:label="@string/title_activity_museum_cafe"
            android:theme="@style/Theme.DbInteractionPractice.NoActionBar"></activity>
        <activity android:name=".receiptPage" />
        <activity android:name=".museumShop" />
        <activity android:name=".museumBookingPage" />
        <activity android:name=".museumPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginPage" />
        <activity android:name=".welcomePage" />
    </application>

</manifest>